

export default function ExerciseContainer(): JSX.Element {

    // provides data for sets
    // passes a set of sets/reps

    // editing during workout changes state here, triggering re-render

    return (
      <>
        
      </>
    )
  }